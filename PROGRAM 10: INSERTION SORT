PROGRAM 10: INSERTION SORT
CODING:
def insertion_sort(arr):
for i in range(1, len(arr)):
key = arr[i]
j = i - 1
while j >= 0 and key < arr[j]:
arr[j + 1] = arr[j]
j -= 1
arr[j + 1] = key
data=input("Enter array to sort:").split(',')
data=list(data)
# Insertion Sort Example
data_insertion = list(data)
print("\nOriginal list for Insertion Sort:", data_insertion)
insertion_sort(data_insertion)
print("Sorted list (Insertion Sort):", data_insertion)
